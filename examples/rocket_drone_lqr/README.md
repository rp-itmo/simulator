# Rocket Drone LQR



Реализация GitHub Issue #34:



**[control] 2d rocket dron**



Дисциплина: **Программирование роботов (ИТМО)**



Алгоритм управления: **LQR (Linear Quadratic Regulator)**



Протокол обмена: **ZeroMQ**

# Цель проекта



Разработать систему управления двухмерной ракетой, способной отслеживать заданную траекторию в плоскости X-Z.



Управление осуществляется через реальные силы двигателей:



* F1 — основной двигатель

* F2 — боковой двигатель

* F3 — боковой двигатель



Ракета должна компенсировать действие гравитации и следовать заданной траектории.

# Математическая модель



Состояние ракеты:
```text

x      положение по оси X

z      положение по оси Z



theta  угол корпуса



vx     скорость по X

vz     скорость по Z



omega  угловая скорость

```

Управление:

```text

F1  основная тяга



F2  боковая тяга



F3  боковая тяга

```



В динамическую модель передаются реальные силы двигателей.

# Архитектура системы

```text

Генератор траектории

&#x20;         |

&#x20;         v

&#x20;    LQR Controller

&#x20;         |

&#x20;         v

&#x20;  Распределение сил

&#x20;         |

&#x20;         v

&#x20;     F1 F2 F3

&#x20;         |

&#x20;         v

&#x20;   Динамика ракеты

&#x20;         |

&#x20;         v

&#x20;      Состояние

&#x20;         |

&#x20;         v

&#x20;       ZeroMQ

&#x20;         |

&#x20;         v

&#x20;    LQR Controller

```

# Структура проекта

```text

rocket_drone_lqr/



communication/

&#x20;   messages.py

&#x20;   zmq_protocol.py



control/

&#x20;   lqr_controller.py



sim/

&#x20;   dynamics.py

&#x20;   limits.py

&#x20;   trajectory.py



tests/

&#x20;   test_dynamics.py

&#x20;   test_lqr_controller.py

&#x20;   test_messages.py

&#x20;   test_zmq_protocol.py



demo_closed_loop.py



run_controller.py

run_simulator.py

```
# Локальный запуск

Запуск симуляции без разделения процессов:

```bash

python -m examples.rocket_drone_lqr.demo_closed_loop

```
# Запуск через ZeroMQ
Окно №1:
```bash

python -m examples.rocket_drone_lqr.run_controller

```

Окно №2:

```bash

python -m examples.rocket_drone_lqr.run_simulator

```

Контроллер и симулятор работают как независимые процессы и обмениваются сообщениями через ZeroMQ.

# Траектория
Реализована траектория типа «восьмёрка» (figure-eight trajectory).

Ракета отслеживает:

```text

x_ref

z_ref

vx_ref

vz_ref

```

# Тестирование

Запуск тестов:

```bash

uv run pytest examples/rocket_drone_lqr/tests

```

Результат:

```text

6 passed

```

Покрыты проверки:

* сериализация сообщений;

* динамика ракеты;

* LQR-контроллер;

* ZeroMQ-протокол.

# Реализованные возможности

* физическая модель ракеты;

* учёт гравитации;

* генератор траектории «восьмёрка»;

* LQR-регулятор;

* распределение управляющих воздействий в силы F1, F2, F3;

* обмен через ZeroMQ;

* запуск контроллера отдельным процессом;

* запуск симулятора отдельным процессом;

* визуализация движения;

* автоматические тесты.

# Алгоритм управления

В проекте используется Linear Quadratic Regulator (LQR).

PID и PD-регуляторы намеренно не используются, поскольку условия задания требуют применения более продвинутого метода управления.

LQR вычисляет оптимальные управляющие воздействия на основе текущего состояния ракеты и требуемого состояния траектории.

# Протокол обмена

Используется схема ZeroMQ REQ/REP.

Сообщения:

```text

ControlRequest

&#x20;   time

&#x20;   state

&#x20;   reference



ControlResponse

&#x20;   F1

&#x20;   F2

&#x20;   F3

```
# Статус проекта

GitHub Issue: #34

Алгоритм: LQR

Протокол обмена: ZeroMQ

Тесты: успешно пройдены

Статус: реализовано
